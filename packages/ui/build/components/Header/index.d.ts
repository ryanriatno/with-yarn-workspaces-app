import React, { ReactNode } from 'react';
import './styles.css';
interface Props {
    onClick: any;
    children?: ReactNode;
}
export declare const Header: React.FC<Props>;
export default Header;
